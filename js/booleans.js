//boolean : true or false

const amIFat = true; //true, false는 값이므로 스트링화 하지 않는다 : 따옴표, 쌍따옴표 안에 쓰지 않는 것.


//null : 아무것도 없음. 빈값. !=false. 아무것도 없음을 지정해주는 값이므로 인위적인 값이다. 자연적으로 나올 수 없음우연이나 뭔가 잘못되서 갑자기 나오는 값이 아님?
//undefined : 정의되지 않음
let something; //변수 선언하되 값을 지정해주지 않음
console.log(something); //정의되지 않음 undefined가 뜸
// 정의된 값 : true, false, null(아무것도 없음으로 변수를 정의함)
// 정의 된지 않은 값ㅁ undefined
// true(켜짐), false(꺼짐), null(비어있음, 빈값이 값임), undefined(정의되지 않음, 값이 없음)는 따옴표 안에 넣지 않느이상 텍스트가 아니고 데이터 타입니다